[phases.setup]
nixPkgs = ["nodejs", "glib", "nss", "nspr", "atk", "cups", "libdrm", "libxkbcommon", "libxdamage", "libxrandr", "mesa", "gtk3", "pango", "cairo", "alsa-lib", "expat", "dbus"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && node src/index.js"
